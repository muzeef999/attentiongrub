{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "run-web" : "cd web && yarn start",
    "test-web" : "cd web && yarn test"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.2.4"
  }
}
